pipeline {
  agent { label 'airship-static-workers' }
  stages {
      stage('Example Build') {
          steps {
              echo 'Hello, Tom'
              sh 'echo "Hello world"'
          }
      }
      stage('Example Test Stage') {
          steps {
              echo 'Hello, Harry'
              sh 'env'
          }
      }
  }
}
